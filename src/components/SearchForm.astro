---
// Если используешь state и обработчики, можно здесь описать логику или передавать пропсы
let searchQuery = '';
const handleSubmit = (e) => {
  e.preventDefault();
  // Тут можно добавить логику поиска
  alert(`Поиск по: ${searchQuery}`);
};
---

<form onSubmit={handleSubmit} class="flex gap-2 max-w-md mx-auto">
  <input
    type="text"
    bind:value={searchQuery}
    placeholder="Введите продукт"
    class="flex-grow p-3 rounded border border-purple-600 bg-purple-900 text-white placeholder-purple-400 focus:outline-none focus:ring-2 focus:ring-purple-500"
  />
  <button
    type="submit"
    class="px-4 py-3 bg-pink-600 hover:bg-pink-700 rounded text-white font-semibold transition"
  >
    Найти
  </button>
</form>
